# =============================================
# PEDAGOGY BACKEND DEPENDENCIES
# =============================================

# FastAPI Framework (Python 3.12 recommended for PaddleOCR support)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Database
sqlalchemy>=2.0.36
aiosqlite>=0.20.0
# asyncpg>=0.30.0  # Uncomment when using PostgreSQL
alembic>=1.14.0

# Pydantic & Settings (latest for Python 3.14)
pydantic>=2.10.0
pydantic-settings>=2.6.0
email-validator>=2.2.0

# Security
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0

# HTTP & Multipart
httpx>=0.28.0
python-multipart>=0.0.17

# Environment
python-dotenv>=1.0.1

# =============================================
# PHASE 3: KNOWLEDGE BASE & RAG SYSTEM
# =============================================

# ChromaDB - Vector Database
chromadb>=0.4.22

# Document Parsing
PyMuPDF>=1.23.0
python-docx>=1.1.0
markdown>=3.5.0
beautifulsoup4>=4.12.0

# Embeddings
sentence-transformers>=2.2.2

# Async File Operations
aiofiles>=23.2.0

# =============================================
# PHASE 4: COMPUTER VISION PIPELINE
# =============================================

# UI Element Detection
# OmniParser v2 (Microsoft) - uses YOLOv8 + Florence-2
ultralytics>=8.0.0
transformers>=4.36.0
huggingface-hub>=0.20.0

# OCR Engines
# Surya OCR - Primary OCR engine (excellent accuracy, fast ~300-500ms)
# Uses PyTorch, models download automatically on first use
surya-ocr>=0.6.0

# PaddleOCR - Fallback OCR engine (~5-10s but very accurate)
# Requires Python 3.8-3.12
paddlepaddle>=2.6.0
paddleocr>=2.9.0
paddle2onnx>=1.2.0  # For converting PaddleOCR models to OpenVINO

# EasyOCR - Legacy fallback OCR engine (kept for compatibility)
easyocr>=1.7.0

# Fast OCR for target verification (~200-400ms vs ~5-10s with PaddleOCR)
# RapidOCR - Optimized PaddleOCR, faster than Tesseract, pure Python
rapidocr-onnxruntime>=1.3.0
rapidocr-openvino>=1.3.0  # OpenVINO-accelerated OCR (~700-800ms)
# Tesseract - Legacy fallback (requires Tesseract binary installed)
pytesseract>=0.3.10
# Windows OCR - Native Windows 10+ OCR API (fastest ~50-200ms)
# winocr>=0.0.7  # Optional - uncomment if Windows 10+ and want faster verification

# Google Cloud Vision OCR - Cloud-based OCR (~2-5s, high accuracy)
# Requires GOOGLE_CLOUD_API_KEY in .env
google-cloud-vision>=3.5.0

# Perceptual hashing for fast page verification
imagehash>=4.3.0

# Image Processing
opencv-python-headless>=4.8.0
Pillow>=10.0.0

# PyTorch (required for OmniParser Florence-2 model)
# CPU version - for GPU, install torch with CUDA support separately
torch>=2.0.0

# Florence-2 Vision-Language Model dependencies
einops>=0.8.0
timm>=1.0.0

# OpenVINO - Intel CPU optimization for YOLO inference (3-4x speedup)
openvino>=2024.0.0

# =============================================
# PHASE 6: AI GUIDANCE ENGINE - LLM PROVIDERS
# =============================================

# Google Gemini API (primary - free tier with good performance)
google-genai>=1.0.0

# =============================================
# PHASE 8: MULTI-APP SUPPORT - WINDOW DETECTION
# =============================================

# Windows UI Automation for browser URL extraction
pywinauto>=0.6.9  # Windows only - extracts URLs from browser address bars
